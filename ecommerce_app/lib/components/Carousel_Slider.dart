import 'package:flutter/material.dart';

class Carusel_Slider extends StatelessWidget {
  const Carusel_Slider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

        // Paste the carousel Code from main shop file
        );
  }
}
